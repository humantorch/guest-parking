version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🔧 REACT_APP_BACKEND_URL=$REACT_APP_BACKEND_URL"
        - npm ci
    build:
      commands:
        - echo "REACT_APP_BACKEND_URL is $REACT_APP_BACKEND_URL"
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
